const UserProfile = (props) => {
     
    return(
        <div>
            <h2>{props.name}</h2>
            <p>Age: {age} </p>
            <p>Bio {bio}</p>
        </div>

    )

}

export default UserProfile; 